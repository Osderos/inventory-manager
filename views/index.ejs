<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1><%= title %></h1>

    <p>
      Welcome to <%= title %>, a website created for keeping track of my models
      collection , to manage and sell various models
    </p>
    <h1>Dynamic content</h1>
    <% if (error==true) {%>
    <p>Error getting dynamic content</p>
    <% } else {%>
    <p>The inventory has the following records :</p>
    <ul>
      <li>Models count: <%= data.model_count %></li>
      <li>Categories count: <%= data.category_count %></li>
    </ul>
    <% } %>
  </body>
</html>
